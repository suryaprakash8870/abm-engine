# Symlink/copy from the root .env in dev. Listed separately so the
# Nest app can also be deployed standalone with its own env file.
DATABASE_URL=postgres://abm:abm@localhost:5432/abm
REDIS_URL=redis://localhost:6379
SECRETS_ENCRYPTION_KEY=
API_PORT=4000
